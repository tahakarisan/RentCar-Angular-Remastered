import { TransformBrandPipe } from './transform-brand.pipe';

describe('TransformBrandPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformBrandPipe();
    expect(pipe).toBeTruthy();
  });
});
